export enum FieldType {
    text = 'text',
    dropdown = 'dropdown',
    dropdownOptions = 'dropdown-options',
    stringDropdown = 'string-dropdown',
    stringDropdownOptions = 'string-dropdown-options',
    numeric = 'numeric',
    checkbox = 'checkbox',
    file = 'file'
}